# نظام الصوت المتقدم - Robust Audio System

## نظرة عامة
تم تطوير نظام صوتي متطور ومقاوم للأخطاء للتطبيق الإسلامي لضمان تشغيل القرآن الكريم بشكل موثوق ودون مشاكل.

## المكونات الرئيسية

### 1. AdvancedAudioEngine (`lib/services/advanced_audio_engine.dart`)
محرك صوتي متقدم مع:
- **نظام إعادة المحاولة التلقائي**: يحاول التشغيل حتى 3 مرات قبل الإفصاح عن الفشل
- **مصادر احتياطية متعددة**: يدعم التبديل بين مصادر صوتية مختلفة
- **فحص الاتصال**: يتحقق من وجود الإنترنت قبل التشغيل
- **معالجة أخطاء شاملة**: يلتقط جميع الأخطاء ويحاول التعافي منها
- **تحكم متقدم**: دعم ضبط الصوت، السرعة، والتكرار

### 2. RobustQuranAudioService (`lib/services/robust_quran_audio_service.dart`)
خدمة صوتية للقرآن مع:
- **قاعدة بيانات خوادم**: تحتوي على روابط احتياطية من مصادر مختلفة (mp3quran.net, everyayah.com)
- **42 قارئ مشهور**: مع روابط صوتية محدثة لكل قارئ
- **استخراج تلقائي للأكواد**: يحدد كود القارئ تلقائياً من الرابط أو الاسم
- **تكوين روابط ذكي**: يبني روابط الصوت بشكل صحيح لكل مصدر

### 3. AudioQuranService (المحدث)
تم تحديثه لاستخدام النظام الجديد بدلاً من المشغل القديم

## المميزات التقنية

### نظام إعادة المحاولة
```
محاولة 1 → فشل → انتظار 1 ثانية
محاولة 2 → فشل → انتظار 2 ثانية
محاولة 3 → فشل → التبديل للمصدر الاحتياطي
```

### مصادر احتياطية
- المصدر الأساسي: mp3quran.net
- المصدر الاحتياطي: everyayah.com
- التبديل التلقائي عند فشل المصدر

### فحص الاتصال
- التحقق من الاتصال قبل كل محاولة تشغيل
- معالجة أخطاء الشبكة بشكل ذكي
- إعادة المحاولة عند انقطاع مؤقت

### معالجة الأخطاء
- التسجيل التفصيلي للأخطاء
- استرداد تلقائي من الأخطاء المؤقتة
- إشعارات المستخدم عند الأخطاء الحرجة

## دعم القراء

يدعم النظام 42 قارئاً مشهوراً من بينهم:
- مشاري العفاسي
- ماهر المعيقلي
- عبد الرحمن السديس
- عبد الباسط عبد الصمد
- محمد صديق المنشاوي
- سعد الغامدي
- ياسر الدوسري
- ... والمزيد

## معلومات التطوير

### التبعيات
```yaml
audioplayers: ^5.2.1
connectivity_plus: ^5.0.2
http: ^1.1.0
```

### الملفات المضافة/المحدثة
- `lib/services/advanced_audio_engine.dart` (جديد)
- `lib/services/robust_quran_audio_service.dart` (جديد)
- `lib/services/audio_quran_service.dart` (محدث)
- `lib/screens/audio_screen.dart` (محدث)

## الاستخدام

### تشغيل سورة
```dart
final audioService = RobustQuranAudioService();
await audioService.playSurah(reciter, surahNumber);
```

### التحكم في التشغيل
```dart
await audioService.pause();
await audioService.stop();
await audioService.seek(Duration(seconds: 30));
await audioService.setVolume(0.8);
await audioService.setSpeed(1.5);
```

### مراقبة الحالة
```dart
audioService.isPlayingStream.listen((isPlaying) {
  print('Playing: $isPlaying');
});

audioService.errorStream.listen((error) {
  print('Error: $error');
});
```

## تحسينات مستقبلية مقترحة

1. **نظام التخزين المؤقت**: تخزين الصوتيات محلياً لتقليل استهلاك البيانات
2. **تنزيل مسبق**: تنزيل السور التالية تلقائياً
3. **جودة متعددة**: دعم جودات صوتية مختلفة (64kbps, 128kbps, 192kbps)
4. **قوائم تشغيل**: إنشاء قوائم تشغيل مخصصة
5. **مزامنة**: مزامنة التقدم بين الأجهزة

## استكشاف الأخطاء

### المشكلة: الصوت لا يعمل
**الحل**: 
1. تحقق من الاتصال بالإنترنت
2. جرب قارئ آخر
3. افحص وحدة التحكم للأخطاء

### المشكلة: تأخير في التشغيل
**الحل**: النظام يحاول عدة مصادر، قد يستغرق بضع ثوانٍ

### المشكلة: الأخطاء المتكررة
**الحل**: قد يكون هناك مشكلة في الخادم، سيحاول النظام المصادر الاحتياطية

## التواصل
للمساعدة أو الأسئلة، راجع ملفات التوثيق الأخرى في المشروع.
